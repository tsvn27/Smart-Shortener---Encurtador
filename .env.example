# ============================================
# SMART SHORTENER - CONFIGURAÇÃO COMPLETA
# ============================================
# Copie este arquivo para .env e configure os valores
# cp .env.example .env

# ============================================
# SERVIDOR BACKEND
# ============================================

# Porta do backend (padrão: 3002)
PORT=3002

# Ambiente: development | production
NODE_ENV=development

# ============================================
# SERVIDOR FRONTEND
# ============================================

# Porta do frontend (padrão: 3000)
FRONTEND_PORT=3000

# URL da API do backend (usado pelo frontend)
NEXT_PUBLIC_API_URL=http://localhost:3002/api/v1

# Domínio dos links encurtados (sem http://)
# Em produção: seu-dominio.com
NEXT_PUBLIC_SHORT_DOMAIN=localhost:3002

# ============================================
# BANCO DE DADOS (MongoDB)
# ============================================

# URI de conexão do MongoDB
# Local: mongodb://localhost:27017/shortener
# Atlas: mongodb+srv://usuario:senha@cluster.mongodb.net/shortener
MONGODB_URI=seu_mongodb_aqui

# ============================================
# SEGURANÇA (OBRIGATÓRIO ALTERAR EM PRODUÇÃO)
# ============================================

# Chave secreta para tokens JWT (mínimo 32 caracteres)
# Gere uma chave segura: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars

# Chave para assinar cookies (mínimo 32 caracteres)
# Gere uma chave segura: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_SECRET=change-this-to-another-secure-random-string

# ============================================
# EMAIL (SMTP) - Para recuperação de senha
# ============================================

# Servidor SMTP (Gmail, Outlook, SendGrid, etc.)
SMTP_HOST=smtp.gmail.com

# Porta SMTP (587 para TLS, 465 para SSL)
SMTP_PORT=587

# Email de autenticação SMTP
SMTP_USER=your-email@gmail.com

# Senha de app (Gmail: https://myaccount.google.com/apppasswords)
# NÃO use sua senha normal, crie uma "Senha de App"
SMTP_PASS=your-app-password

# Email que aparece como remetente
FROM_EMAIL=noreply@yourdomain.com

# ============================================
# URLs DA APLICAÇÃO
# ============================================

# URL do frontend (para links de recuperação de senha)
APP_URL=http://localhost:3000

# Origens permitidas para CORS (separar por vírgula se múltiplas)
CORS_ORIGIN=http://localhost:3000

# ============================================
# LOGS
# ============================================

# Nível de log: error | warn | info | debug
LOG_LEVEL=info
